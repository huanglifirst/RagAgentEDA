# 恒生 AI 应用开发工程师面试准备

## 1. 先给面试官的项目一句话

我做的这个项目叫 `RagAgentEDA`，本质上是一个面向 EDA 场景的 `RAG + 可控Agent编排 + 测试脚本生成/执行` 后端原型。它不是通用聊天机器人，而是围绕 TED 用户文档，把“查文档 -> 理解需求 -> 生成测试脚本 -> 可选执行 -> 返回指标”这条链路工程化打通。

如果只讲重点，我会这样说：

1. 我把 TED 文档做了结构化切块和向量化，构建了一个面向 EDA 语料的 RAG 检索链路。
2. 在这个基础上我做了两个能力：
   - 一个是 `TED文档问答助手`，专门回答 TED 相关问题；
   - 一个是 `任务执行链路`，把用户自然语言需求转成测试脚本，必要时执行并返回结果。
3. 这个项目里我重点负责的是 Python 后端、RAG 流程、模型 API 封装、检索降级、工程化可观测性和执行链路闭环。

这套说法和恒生岗位 JD 是高度匹配的，因为岗位本身就强调：

1. Python 开发
2. 大模型 API 调用与封装
3. RAG / 智能助手落地
4. FastAPI 工程化
5. 提效工具研发与迭代

---

## 2. 面试开场版项目介绍

### 2.1 30 秒版本

这个项目是一个面向 EDA 场景的 AI 应用原型。系统先从 `Resource/` 里的 TED 文档中做检索，再把检索到的证据交给大模型，生成测试脚本；如果打开执行模式，还会通过本地或 SSH 在目标环境执行脚本并返回指标。除此之外，我还做了一个单独的 `TED文档问答助手`，用于严格基于证据回答 TED 文档问题，减少工程师手工翻文档的时间。

### 2.2 1 分钟版本

这个项目分成两条主线。

第一条主线是 `/v1/tasks/run`，它更像一个面向任务的 Agent 工作流：用户输入“请测试该运放电路的带宽”，系统会先做 RAG 检索，找到 TED 文档里和带宽测试、自动测量、AC 分析相关的证据，然后把这些证据交给 LLM 去生成 Python 测试脚本；如果 `execute=true`，就进一步做 preflight 检查、执行脚本、校验 `result.json`，最终把 `bandwidth_hz` 等指标返回。

第二条主线是 `/v1/rag/ask`，也就是 `TED文档问答助手`。它更强调“基于证据回答”而不是代码生成。这个模块做了运行时缓存、向量检索 + BM25 补充召回、二阶段 rerank、低置信度拒答、证据可视化和用户反馈闭环，目标是让问答结果更稳，不要乱编 API、类名和参数。

### 2.3 3 分钟版本

如果面试官让我展开，我会按“业务问题 -> 技术方案 -> 工程化细节”讲。

业务问题是：TED 文档很多，而且是 HTML 文档站点，工程师经常需要先翻文档，再写测试脚本，再跑测试。这个流程重复、耗时，而且容易漏掉 API 细节。

我的方案是把这件事拆成两层：

1. 第一层是知识获取，也就是 RAG。
   - 先把 `Resource/` 目录下的 `md/markdown/html/htm/txt` 文档统一解析；
   - 对 HTML 不只是去标签，而是做正文抽取、噪声过滤、标题分段、表格转 markdown；
   - 再做 embedding、持久化索引、混合检索和 rerank。

2. 第二层是任务编排。
   - 对文档问答场景，走 `问答助手`；
   - 对“生成测试脚本”的场景，走 `LangGraph` 编排的 `prepare -> retrieve -> codegen -> preflight -> execute` 工作流。

这个项目不是强调“自治智能体”，而是强调“可控工作流”。我在面试里会主动说明：这里的 Agent 更准确地说是一个可控的状态机式编排，而不是放任模型自由调用工具的黑盒系统。这种说法会更工程化，也更容易获得面试官认可。

---

## 3. 项目整体架构

### 3.1 模块划分

核心模块我会这样讲：

1. `backend/app.py`
   - FastAPI 服务入口
   - 对外暴露 `/health`、`/v1/rag/reindex`、`/v1/tasks/run`、`/v1/rag/ask`
   - 同时挂载了 Gradio 页面 `/ragagent`

2. `backend/config.py`
   - 统一加载 `.env`
   - 管理 chat / embedding / rerank / execution 相关配置

3. `backend/rag/`
   - `indexer.py`：文档扫描、HTML 清洗、分段、切块
   - `vector_store.py`：embedding、向量索引读写、向量检索
   - `retriever.py`：BM25-like + overlap 的 lexical 检索

4. `backend/agents/`
   - `orchestrator.py`：LangGraph 工作流编排
   - `codegen.py`：根据 evidence 生成测试脚本
   - `qa_agent.py`：TED 文档问答助手

5. `backend/llm/client.py`
   - OpenAI 兼容接口封装
   - 对接 `/chat/completions`、`/embeddings`、`/rerank`、`/models`

6. `backend/runner/`
   - `local_runner.py`：本地执行
   - `ssh_runner.py`：远程执行

7. `backend/storage/qa_feedback_store.py`
   - SQLite 反馈日志
   - 保存问答记录和“有用 / 无用”反馈

### 3.2 两条核心链路

#### 链路一：任务执行链路

用户请求：

```json
{
  "query": "请测试该运放电路的带宽",
  "circuit_description": "两级运放，输出带负载",
  "top_k": 6,
  "execute": false
}
```

系统执行：

1. `prepare`：创建任务目录和 `task_id`
2. `retrieve`：RAG 检索 evidence
3. `codegen`：让模型生成 `main.py`
4. `preflight`：检查 bashrc、命令、Python 模块
5. `execute`：执行脚本并校验 `result.json`

#### 链路二：TED 文档问答助手

用户请求：

```json
{
  "question": "TED中如何设置PVT环境？"
}
```

系统执行：

1. 准备 runtime assets：chunk、embedding retriever、lexical retriever
2. 做混合检索和 rerank
3. 根据检索分数与 query-evidence overlap 先判断是否该拒答
4. 如果通过门控，再基于 evidence 让模型生成最终答案
5. 记录 evidence、warning、耗时日志和用户反馈

---

## 4. 你要重点讲透的 RAG 部分

这是整个项目最值钱的部分，也是最贴恒生岗位的部分。

### 4.1 为什么要做 RAG

我的回答建议这样说：

TED 文档本身是外部知识，而且是会更新的。如果直接把这些知识硬塞进 prompt，成本太高；如果做微调，又有版本更新和知识失效的问题。RAG 更适合这个场景，因为：

1. 知识更新频率高，重建索引比重新训练快得多
2. 业务里有大量 API 名、类名、参数名，适合“检索后生成”
3. 需要给出 evidence，便于工程师核对
4. 对内网部署来说，本地索引可控、成本低

一句话总结就是：

**这个项目里 RAG 的价值，不只是让模型更“懂”TED，而是让回答和生成都能“有出处”。**

### 4.2 数据源是什么

知识源主要是 `Resource/` 目录下的 TED 文档，包括：

1. TED 前端
2. TED 电路开发流程
3. TED 版图
4. 生成器和测试器
5. TED 内核 API
6. 参考设计
7. 补充的 `docs/ted_frontend/automated_measurement_measure.md`

这点很重要，因为面试时你可以强调：

**我的语料不是纯文本 FAQ，而是 HTML 站点文档 + API 文档 + 示例代码 + 业务说明混合语料。**

### 4.3 文档解析不是简单切字符串

源码里 `ResourceIndexer` 做得比较细，这里非常值得讲。

#### 4.3.1 HTML 清洗策略

我不是直接把 HTML 去标签，而是做了下面几件事：

1. 识别正文容器
   - 如 `main-content`、`theme-hope-content`、`article-body`、`markdown-body`
2. 过滤噪声节点
   - `script`、`style`、`nav`、`aside`、`header`、`footer`、`svg`
   - 还会根据 `navbar`、`sidebar`、`breadcrumb`、`slimsearch` 等 marker 过滤模板噪声
3. 解析标题层级
   - 把 `h1~h6` 维护成 `heading_stack`
   - 最后形成类似 `Section: A > B > C` 的上下文前缀
4. 表格特殊处理
   - HTML table 会转成 markdown table
   - 这样检索和 evidence 展示时能保留参数表结构

这里如果面试官追问“为什么要这么做”，你可以回答：

因为 TED 文档里有大量 API 参数表、测试器说明和嵌套章节，如果简单 strip tags，会把结构全打平，检索效果会明显下降。

#### 4.3.2 切块策略

这里要说源码真实值，不要背 README 老参数。

当前代码中的切块策略是：

1. 先按标题和段落做 section 切分
2. 每个 chunk 默认 `size=850`、`overlap=150`
3. 过长段落会按句子继续细分
4. 长度小于 `50` 的片段直接丢弃

我会这样解释这个设计：

1. `850` 不是越大越好，因为 chunk 太大容易混入多个主题
2. `150 overlap` 是为了避免跨段边界导致语义断裂
3. 在 EDA/API 文档场景里，段落边界和标题层级通常比固定字符切分更重要

### 4.4 索引构建与持久化

#### 4.4.1 fingerprint 机制

索引不是随便命名的，而是根据文档的：

1. 文件路径
2. 文件大小
3. `mtime_ns`

计算 `sha256 fingerprint`。

这样做的好处是：

1. 只要文档内容没变，就能复用旧索引
2. 文档变了就自动失效重建
3. 不需要额外维护复杂版本号

#### 4.4.2 持久化格式

当前没有上 Milvus / FAISS 服务，而是把索引落成 JSON：

1. 保存路径：`workdir/vector_index/<fingerprint>.json`
2. 最新索引指针：`workdir/vector_index/LATEST`
3. payload 包含：
   - `fingerprint`
   - `chunk_count`
   - `vector_dim`
   - `chunks`
   - `vectors`
   - `embedding_model`
   - `embedding_api_base`
   - `created_at`

这也是一个很好的面试点：

**我没有一上来就引入重量级向量数据库，而是根据语料规模和项目阶段选择了更轻的 JSON 持久化方案。**

如果面试官问“为什么不直接上 Milvus”，你可以这样答：

1. 这是 Phase-1 Demo，优先验证链路
2. 语料是本地静态 TED 文档，规模可控
3. JSON 本地持久化更利于内网落地和调试
4. 等后续语料量和并发上来，再平滑切到 FAISS / Milvus

### 4.5 Embedding 层设计

这里不要只说“调了 embedding API”，而要讲工程细节。

#### 4.5.1 模型与接口解耦

项目里 chat、embedding、rerank 是分开的：

1. Chat 模型：`MODEL_NAME`
2. Embedding 模型：`EMBEDDING_MODEL_TEXT`
3. Rerank 模型：`RERANK_MODEL_TEXT`

这说明我在设计上做了 provider decoupling，而不是把所有能力绑死在一个模型上。

#### 4.5.2 embedding 批处理

源码里 embedding 有几个关键细节：

1. 每个 chunk 文本最多截到 `3000` 字符
2. 默认 `batch_size=10`
3. 默认 `retry_count=4`
4. 默认指数退避重试
5. 如果 API 返回“batch size not be larger than N”，会自动缩小 batch 大小再重试

这部分是非常好的工程化亮点，因为它说明你考虑过真实 API 的限流、超时和载荷上限，而不是只在 happy path 上跑通。

### 4.6 检索链路为什么是混合检索

这是面试官非常爱问的点。

#### 4.6.1 第一阶段：向量召回

向量检索用 cosine similarity 做相似度计算。

优点：

1. 能处理语义表达差异
2. 适合中文自然语言问题
3. 对“运放带宽怎么测”这类意图型问题更友好

#### 4.6.2 补充阶段：Lexical broad retrieval

项目没有只依赖 embedding，而是额外做了 lexical broad retrieval。

`HybridRetriever` 里用了一个 BM25-like 评分：

1. `bm25_like * 0.7`
2. `token overlap * 0.3`

这里的业务意义是：

TED 文档里有很多强标识符，比如：

1. `Measure.ac`
2. `test_pvt`
3. `setenv`
4. `layout_mos`
5. `SmartBus`

这些关键词如果只靠向量，有时不如 lexical 匹配稳定。所以我做的是：

**向量召回负责语义，BM25 负责精确命中 API 名和术语。**

#### 4.6.3 候选集合融合

项目里会把 vector candidates 和 lexical candidates 合并去重：

1. 以 `chunk_id` 去重
2. 合并上限是 `2 * rerank_top_n`
3. 如果最终候选为空，就 fallback 到全量 corpus

这套逻辑你可以讲成：

**召回阶段宁可多给候选，不要过早截断；真正的精排交给 rerank。**

### 4.7 第二阶段 rerank

项目里默认启用 rerank。

逻辑是：

1. `rerank_top_n = max(20, top_k * RERANK_TOPN_FACTOR)`
2. 默认 `top_k=6`
3. 默认 `RERANK_TOPN_FACTOR=4`
4. 所以常见情况下 rerank 输入规模是 `24`

为什么这样设计：

1. top_k 太小会导致召回不足
2. rerank 输入太大又会增加时延和成本
3. `24` 对当前语料和任务是一个折中值

如果 rerank 调用失败，系统不会直接挂，而是：

1. 记录 warning
2. 降级到 lexical rerank fallback

这就是面试里很加分的一句：

**我不是只做了“最优路径”，还设计了“失败时怎么继续服务”。**

### 4.8 RAG 结果怎么控制幻觉

这个项目里防幻觉不是靠一句 prompt，而是多层控制。

#### 4.8.1 提示词约束

问答 prompt 明确要求：

1. 只能基于给定证据回答
2. 不要引入证据中不存在的 API、类名或参数
3. 优先回答证据能确认的部分
4. 证据不足时只回答“未检索到相关内容”

#### 4.8.2 检索前门控

`qa_agent.py` 里有一个很重要的 pre-abstain 策略：

当满足下面条件时直接拒答：

1. `qa_overlap < 0.15`
2. 或者 `top1_score < 0.70 且 qa_overlap < 0.30`

这说明系统不是“检索到一点点就硬答”，而是先判断 evidence 是否足够支撑回答。

#### 4.8.3 模型后门控

如果模型输出空答案，或者仍然返回“未检索到相关内容”，系统会标记 `model_abstain`。

#### 4.8.4 失败时保守降级

仓库现有测试产物里，可以看到一轮在 Windows 受限网络环境下，embedding 和 rerank 都因为 `WinError 10013` 不可用，系统退化成纯 BM25 检索，并触发 `pre_abstain_low_retrieval` 返回“未检索到相关内容”。这其实是个很好的面试素材，因为它说明：

**系统在弱网或依赖故障时，优先保证“不乱答”，而不是强行输出看起来像正确的答案。**

---

## 5. TED 文档问答助手怎么讲

这是你面试里要重点讲的第二部分。

### 5.1 它和普通 RAG 问答有什么不同

我建议你这样答：

这个问答助手不是面向通用百科，而是一个垂直领域问答助手，语料是 TED 的 API 文档、测试器文档、参考设计和开发流程说明，所以我在系统设计上更强调：

1. 证据可追溯
2. API / 参数级精确匹配
3. 低置信度拒答
4. 问答日志和用户反馈闭环

### 5.2 运行时缓存

问答助手不是每次都重建资源。

它会缓存：

1. chunks
2. fingerprint
3. embedding retriever
4. lexical retriever

并且：

1. 用 `Lock` 保证线程安全
2. `reindex` 之后会 `invalidate_cache`
3. 如果开启 `rag_qa_warmup_on_reindex`，会在重建索引后预热缓存

这块可以说成：

**我做了问答链路的冷启动优化，避免每次都重新扫描语料和构建运行时对象。**

### 5.3 回答时的证据组织

问答助手回答时：

1. 默认取前 `6` 条 evidence
2. 传给 LLM 时通常只取前 `4` 条
3. 每条 evidence 会截断到大约 `450` 字符

这里体现的是 token 成本控制思路。

如果面试官问“token 机制在项目里怎么体现”，你可以这样答：

1. chunk 本身做了大小控制
2. rerank 后只保留 top-k
3. 最终喂给大模型的 evidence 再次截断
4. 目的是在保证证据密度的前提下，把 token 控制在可接受范围内

### 5.4 UI 层面做了什么

这个项目不仅有 API，还有 Gradio UI，名字就是 `TED文档问答助手`。

我会强调两个点：

1. evidence 可视化
   - 会把 snippet 做文本 / 代码 / markdown table 的智能渲染
   - 对参数表、代码段、普通说明分别展示
2. 用户反馈闭环
   - 用户可以点“有用 / 无用”
   - 写入 SQLite `qa_logs`

这说明项目不是“后端 demo 到此为止”，而是考虑了真实使用时的可解释性和反馈采集。

### 5.5 反馈闭环设计

`qa_feedback_store.py` 里用了 SQLite + WAL：

表结构包括：

1. `question`
2. `answer`
3. `status`
4. `warning`
5. `evidence_json`
6. `source`
7. `created_at`
8. `feedback`
9. `feedback_updated_at`

这在面试里可以讲成：

**我给 RAG 问答做了最基本的 data flywheel，后续可以基于这些反馈做 bad case 分析、重排优化甚至构建评测集。**

### 5.6 可观测性

问答助手每次调用会打 timing log：

1. `assets_prepare_ms`
2. `vector_search_ms`
3. `lexical_broad_ms`
4. `rerank_ms`
5. `answer_llm_ms`
6. `total_ms`

这句很适合面试：

**我做的不只是功能，还拆分了检索、重排、生成的耗时，便于后续做性能瓶颈定位。**

---

## 6. 任务生成与执行链路怎么讲

这部分能体现你不只是做问答，还做了“Agent + 工具执行”。

### 6.1 LangGraph 为什么适合这个项目

这里建议讲得克制一点，不要夸大。

可以这样回答：

我用了 LangGraph，但不是为了做“完全自治的 Agent”，而是为了把任务流程显式建模成图。当前主流程是固定的：

1. `prepare`
2. `retrieve`
3. `codegen`
4. `preflight`
5. `execute`

这样做的好处是：

1. 节点职责清晰
2. 状态可显式传递
3. 后续要扩展人工审核、重试、并行工具节点比较方便

### 6.2 codegen 怎么做

codegen 不是让模型随便写，而是先根据 query 推断测试类型：

1. 包含 `sfdr` -> `dac_sfdr`
2. 包含 `loop` / `环路` -> `amp_loop_gain`
3. 否则默认 `opamp_bandwidth`

然后：

1. 先生成 plan text
2. 再把 query、circuit_description、evidence、metric key、方法提示一起交给模型
3. 要求模型只返回单个 Python 脚本
4. 脚本必须包含 `run_test() -> dict`
5. `__main__` 中必须写出 `result.json`

这里你可以强调：

**我在生成侧做了结构约束，不是简单让模型“帮我写代码”，而是让它产出符合执行器契约的脚本。**

### 6.3 execute=true 为什么只支持带宽

当前源码里真实执行模板只接了 `opamp_bandwidth`。

`execute=true` 时：

1. 如果不是 `opamp_bandwidth`，直接报错
2. 如果是带宽任务，则进一步提取：
   - `input_node`
   - `output_node`
   - `start_hz`
   - `stop_hz`
   - `points`
   - `target_gain_db`

默认值分别是：

1. `vin`
2. `vout`
3. `1.0`
4. `1e9`
5. `200`
6. `-3.0`

然后渲染成真实模板，调用环境变量 `TED_BANDWIDTH_CMD` 指向的命令执行。

这块很适合解释项目当前边界：

**我已经把“从 RAG 到真实执行”的闭环打通了，但目前真实模板只落到带宽场景，其他任务类型还停留在生成预览脚本阶段。**

这不是缺点，反而是你非常诚实、非常工程化的体现。

### 6.4 执行器怎么保证结果可信

执行器不是只看进程退出码，而是有结果契约。

`result.json` 必须满足：

1. root 是 object
2. 有非空 `status`
3. 有 `metrics`
4. `metrics` 必须包含 `bandwidth_hz`
5. `bandwidth_hz` 必须可转 float
6. `notes` 必须是 list
7. 如果 `status=PASS`，则 `bandwidth_hz > 0`

这块是非常典型的工程化点：

**我没有把“脚本跑完”当成功，而是把“结果结构合法且指标可信”当成功。**

### 6.5 本地执行与 SSH 执行

项目支持两种执行模式：

1. `local`
2. `ssh`

两种模式都会先做 preflight：

1. `source ~/.bashrc`
2. 检查必需命令
3. 检查必需 Python 模块

这点很贴恒生 JD 里的 Linux / 工程环境要求，你可以自然地说：

**我不仅做了模型和 RAG，还考虑了实际执行环境的依赖检查和远程运行场景。**

---

## 7. 这个项目里最能打动面试官的工程细节

### 7.1 我会重点强调的 8 个细节

1. 不是简单切文本，而是做了 HTML 主内容识别、模板噪声过滤、表格转 markdown。
2. 不是单路检索，而是 `embedding + lexical + rerank` 混合检索。
3. 不是只考虑成功路径，而是对 embedding/rerank 都做了 fallback。
4. 不是只让模型回答，而是做了 pre-abstain 和 model-abstain 双层拒答。
5. 不是只做接口，而是做了 Gradio UI、evidence 展示和用户反馈记录。
6. 不是只做生成，而是把 `execute=true` 的真实执行链路和结果契约打通。
7. 不是每次都重建资源，而是做了 runtime cache 和 warmup。
8. 不是纯 demo，而是配了 smoke check、retrieval regression 和 QA 测试产物。

### 7.2 可以主动讲的 trade-off

如果面试官喜欢问“为什么这么设计”，你可以这样答：

1. 为什么不用重量级向量库？
   - 因为当前语料是静态本地 TED 文档，先用 JSON 索引更轻、更方便内网调试。

2. 为什么要 lexical supplement？
   - 因为 EDA 场景里有大量 API 名和缩写，纯向量容易错过精确命名。

3. 为什么要拒答？
   - 在企业内部知识助手场景里，“答错”比“暂时不答”风险更高。

4. 为什么用 LangGraph？
   - 不是为了炫技，而是为了把状态和节点职责显式化，便于后续扩展。

5. 为什么执行器要验证 `result.json`？
   - 因为 LLM 生成和脚本执行都可能看似成功，但结果结构未必可信。

---

## 8. 结合恒生岗位要求，怎么把这个项目说成你的优势

### 8.1 Python / FastAPI

你可以直接说：

这个项目的服务层就是用 FastAPI 搭的，接口层包括健康检查、重建索引、问答和任务执行。对我来说，这不是只会写脚本，而是完整地做了一个可部署的 Python 后端服务。

### 8.2 大模型 API 调用与封装

项目里我把 chat、embedding、rerank 都封装在 `OpenAICompatClient` 里，统一处理：

1. base_url
2. api_key
3. payload 结构
4. 错误处理

所以我不是只会调一个 `/chat/completions`，而是把整套 LLM 相关能力抽象成了可替换的客户端。

### 8.3 RAG / 智能助手

这个项目同时覆盖了：

1. RAG 检索
2. TED 文档问答助手
3. 面向任务的脚本生成助手

这和 JD 里写的 `RAG系统、智能助手` 是完全贴合的。

### 8.4 数据处理能力

文档清洗、切块、向量持久化、候选融合、评分排序，这些都属于比较扎实的数据处理能力，不只是前端调接口。

### 8.5 工程落地能力

我会特别强调：

1. fallback
2. cache
3. warmup
4. timing log
5. smoke check
6. regression
7. SQLite feedback
8. SSH 执行

这些都说明我具备把 AI 应用从 demo 做到“可用”的意识。

---

## 9. 大模型面试八股文，结合这个项目怎么回答

下面这部分是面试时最实用的。

### 9.1 什么是 RAG？你这个项目里为什么适合用 RAG？

标准回答：

RAG 是 Retrieval-Augmented Generation，也就是“检索增强生成”。先从外部知识库检索相关内容，再把检索结果和用户问题一起交给大模型生成答案。它适合知识更新频繁、需要可追溯证据的场景。

项目化回答：

在我这个项目里，TED 文档本身是频繁更新的外部知识，而且包含大量 API 名、参数名、测试器说明和参考设计。如果做微调，更新成本高，而且很难保证答案能回溯到具体文档；而用 RAG，我只要重新索引文档，就能让系统获得最新知识，还能把 evidence 返回给用户。

一句话版：

**我这个项目里用 RAG，不是为了让模型“更聪明”，而是为了让它“回答有依据、更新更轻量”。**

### 9.2 RAG 和微调的区别

标准回答：

1. RAG 是把知识放在外部知识库，推理时动态检索
2. 微调是把模式或能力写进模型参数
3. RAG 更适合知识更新快的场景
4. 微调更适合固定任务风格或行为模式对齐

结合项目回答：

TED 文档更新后，我不需要重新训练模型，只需要重新跑 `/v1/rag/reindex`。所以这个项目明显更适合 RAG 而不是微调。未来如果我要优化的是“脚本生成风格”或者“企业内部话术风格”，那可能才会考虑轻量微调或 SFT。

### 9.3 什么是 embedding？什么是 rerank？两者关系是什么？

标准回答：

1. Embedding 是把文本映射到向量空间，便于做语义相似度检索
2. Rerank 是对召回候选做精排，判断哪个文档对当前 query 更相关
3. embedding 负责广召回，rerank 负责高精度排序

结合项目回答：

在我的项目里，embedding 用来从 TED 文档 chunk 中找语义相关候选，rerank 用于从这些候选里做二阶段精排。因为 EDA 场景里 API 名和术语很多，我还加入了 lexical broad retrieval 做补充召回，这样形成 `向量召回 + BM25补充 + rerank精排` 的混合检索链路。

### 9.4 为什么不是只用向量检索？

项目化回答：

因为纯向量检索对显式 API 名、类名、参数名不一定稳定。比如 `Measure.ac`、`test_pvt`、`layout_mos` 这种强标识符，BM25 或关键词匹配经常更可靠。所以我在项目里保留了 lexical supplement，让语义召回和精确命中互补。

### 9.5 chunk 为什么不能太大或太小？

标准回答：

1. 太大：主题混杂，检索不精确，token 成本高
2. 太小：上下文不足，语义割裂
3. 所以需要根据文档结构和任务做折中

结合项目回答：

我的项目里不是简单定长切，而是先按标题和段落做 section，再按 `850/150` 切块。因为 TED 文档有很多层级标题和参数表，如果忽略结构，检索命中率会明显下降。

### 9.6 token 机制在项目里怎么体现？

项目化回答：

token 机制在这个项目里主要体现在三个地方：

1. chunk 大小控制，避免单块过长
2. rerank 后只保留 top-k evidence
3. 最终喂给模型时只截取前几条 evidence，每条再做长度裁剪

这样做的目的就是在有限上下文窗口内提高信息密度，减少无关 token 浪费。

### 9.7 temperature 是什么？你为什么设成 0.1？

标准回答：

temperature 控制采样随机性。越低越稳定，越高越发散。

结合项目回答：

我的项目里无论是 QA 还是 codegen，temperature 都偏低，因为这是企业工具场景，我更希望输出稳定、可复现，而不是创意发散。尤其是问答助手和脚本生成器，都更强调正确性而不是多样性。

### 9.8 prompt engineering 你在项目里怎么做的？

结合项目回答：

我做 prompt 的原则不是“写很花的提示词”，而是做任务约束。

比如问答助手的 system prompt 会明确要求：

1. 只能基于证据回答
2. 不要发明证据外的 API、类名、参数
3. 证据不足就拒答

而脚本生成 prompt 会明确要求：

1. 只输出单个 Python 脚本
2. 必须有 `run_test() -> dict`
3. 必须输出 `result.json`
4. 返回结构里必须包含指定 metric key

所以我做 prompt 更多是做结构约束，而不是追求复杂 wording。

### 9.9 幻觉怎么处理？

建议回答：

我在这个项目里是多层处理幻觉：

1. 检索层：向量 + lexical + rerank 提高证据质量
2. 门控层：低分和低 overlap 时直接拒答
3. 生成层：prompt 严格要求只基于证据
4. 展示层：把 evidence 暴露给用户
5. 反馈层：用户可标记“有用/无用”

我不认为只靠 prompt 就能解决幻觉，必须把检索、门控、输出和反馈结合起来。

### 9.10 你理解的 Agent 是什么？这个项目算真正的 Agent 吗？

这是个高质量回答点。

建议你说：

我认为 Agent 不一定非得是完全自治的工具调用系统。在工程上，更重要的是“模型是否参与决策”和“流程是否可控”。我这个项目里的 `/v1/tasks/run` 更接近一个可控的 Agent Workflow：检索、代码生成、执行都是按图编排的，模型主要参与理解需求和生成脚本，而不是完全自由探索。

如果更严格一点说，它是 `LangGraph 驱动的可控工作流`，比“全自动 Agent”更适合企业内部工具场景。

### 9.11 上下文学习和微调有什么区别？

标准回答：

上下文学习是模型在推理时依赖 prompt 和上下文临时学习；微调是把任务模式写进参数。

结合项目回答：

我的项目更多依赖上下文学习，也就是把检索到的 TED 证据放进 prompt，让模型在当前上下文里回答或生成脚本，而不是通过训练永久写进模型参数。

### 9.12 模型选型你怎么考虑？

建议回答：

我会把模型拆成三类能力看：

1. chat model：负责回答和代码生成
2. embedding model：负责语义召回
3. rerank model：负责精排

我这个项目就是这么做的。这样一方面解耦能力，另一方面也方便后续替换服务商或模型版本。

### 9.13 如何评估 RAG 效果？

标准回答：

RAG 评估通常看：

1. 召回质量
2. 排序质量
3. 最终答案正确性
4. 幻觉率
5. 拒答精度

结合项目回答：

我的项目里已经有几层评估：

1. `smoke_check.py`
   - 校验服务和重建索引基本可用
2. `workdir/regression/retrieval_regression_cases.json`
   - 对带宽、GBW/PM、SFDR、loop gain、Measure.ac 等核心场景做检索回归
3. `run_qa_agent_test.py`
   - 会记录 `unsupported_claim_rate`、`abstain_precision`、`topic_drift_rate`

这能说明我不是“感觉效果还行”，而是有意识地把 RAG 评测结构化了。

### 9.14 企业里最重要的不是答得多，而是答得对，你怎么理解？

建议回答：

我非常认同。尤其是企业内部知识助手场景，错误答案会直接误导开发和使用，所以我在项目里宁可做拒答，也不希望系统在低置信度下强行回答。像 `pre_abstain_low_retrieval` 这样的策略，本质上就是在把“正确率优先”落到工程实现里。

---

## 10. 面试官大概率会追问的项目细节题

### 10.1 你这个项目里最难的点是什么？

建议回答：

我觉得最难的点不是“把接口调通”，而是让系统在垂直文档场景里稳定工作。因为 TED 文档是 HTML 站点，结构复杂，还有很多参数表、代码示例和 API 说明。如果文档解析和切块做不好，后面的 embedding、rerank、回答都会被污染。所以我在数据清洗和结构化切块上花了不少精力。

### 10.2 你做过哪些降级和容错？

建议回答：

我做过几层：

1. embedding 失败时，自动走 lexical supplement
2. rerank 失败时，自动走 lexical rerank fallback
3. 检索置信度不足时，直接拒答
4. 代码执行前先做 preflight
5. 执行后校验 `result.json` 合法性

### 10.3 你怎么证明这个项目是工程化落地，而不是实验脚本？

建议回答：

因为它有完整的服务接口、配置中心、索引持久化、运行时缓存、UI 页面、反馈落库、执行器、健康检查和回归脚本。实验脚本通常只关注功能能不能跑，这个项目已经开始关注稳定性、可观测性和迭代闭环。

### 10.4 如果让你继续优化，你下一步做什么？

建议回答：

我会优先做四件事：

1. 把 JSON 索引升级成更成熟的向量存储，比如 FAISS 或 Milvus
2. 扩展真实执行模板，不只支持带宽，还支持 SFDR、环路增益等
3. 基于现有 `qa_logs` 和 bad case 继续优化 chunking、rerank 和拒答阈值
4. 增加更系统化的离线评测和在线观测

---

## 11. 项目里的业务细节，面试时可以顺手带出来

这部分非常适合体现你真的看过语料，而不是只会讲框架。

### 11.1 关于自动测量

在 `automated_measurement_measure.md` 里，可以检索到运放 AC 分析相关指标集合，比如：

1. `gain`
2. `gbw`
3. `pm`
4. `bw`
5. `ugb`
6. `maxgain`

所以当我做“运放带宽”或“GBW 和相位裕度怎么测”这类 query 时，目标不是做泛泛回答，而是把 query 召回到自动测量和 AC 分析相关证据上。

### 11.2 关于 PVT

语料里不仅有 `test_pvt` 这种 API 说明，还有 `setenv(PVT=...)` 这种环境设置说明。这意味着用户问“PVT 怎么配置”，系统应该优先检索到：

1. PVT 测试 API
2. `setenv` 的环境配置语义
3. `simulator='spectremdl'` 之类的仿真环境设置

### 11.3 关于 TED 的自动化价值

从 TED 文档里可以看出，它强调的是：

1. 全代码化描述
2. 从网表转 TED 代码
3. 自动布局与自动布线
4. 与 Virtuoso 等环境集成

所以这个 AI 项目的业务定位并不是取代 TED，而是降低工程师使用 TED 的门槛，提高“查文档 + 写测试脚本”的效率。

---

## 12. 可以直接背的“项目亮点总结”

我建议你背下面这版，简洁而且很稳。

这个项目最大的亮点有四个。

第一，我做的是垂直领域 RAG，不是通用问答。语料是 TED 的 HTML 文档、API 文档和参考设计，所以我在文档解析时专门做了正文抽取、噪声过滤、标题感知切块和表格保留。

第二，我做的是混合检索，不是单一路径。检索链路是向量召回、BM25 补充召回和 rerank 精排，既照顾语义理解，也照顾 API 名和参数名这种精确术语。

第三，我做了比较完整的防幻觉设计。包括 evidence 约束 prompt、低置信度拒答、embedding/rerank 失败降级，以及 evidence 可视化，目的是尽量避免系统在企业场景里乱答。

第四，我把链路工程化了。除了问答助手，我还把“检索 -> 代码生成 -> preflight -> 执行 -> 指标返回”这条链路打通，并且做了健康检查、回归测试、缓存预热和反馈落库。

---

## 13. 最后一句收尾

如果面试官问“你觉得这个项目最能体现你什么能力”，我建议这样收尾：

我觉得这个项目最能体现我三点能力：

1. 我能把大模型能力和真实业务场景结合起来，而不是只会调一个聊天接口；
2. 我能把 RAG、后端服务、执行链路和质量控制整合成一个完整系统；
3. 我在做 AI 应用时会优先考虑工程可用性，比如容错、拒答、反馈和评测，而不是只追求 demo 看起来能跑。

如果岗位需要的是“会用 Python 把大模型能力落到内部提效工具里”，那这个项目和这个岗位是非常匹配的。

---

## 14. 1 分钟口语化讲解稿

下面这版可以直接背，适合面试官说“你简单介绍一下这个项目”时使用。

### 14.1 60 秒版本

我做的这个项目叫 `RagAgentEDA`，它是一个面向 EDA 场景的 AI 应用原型，核心是把 TED 用户文档和大模型结合起来，解决工程师“查文档、写测试脚本、跑测试”这个重复流程。

整个系统分两条主线。第一条是 `TED文档问答助手`，用户可以直接问 TED 相关问题，系统会先从本地 TED 文档里做检索，再基于证据回答，而且我做了低置信度拒答，避免乱编 API 和参数。第二条是任务执行链路，用户比如输入“请测试该运放电路的带宽”，系统会先做 RAG 检索，再生成 Python 测试脚本；如果打开执行模式，还会做环境检查、执行脚本、校验结果并返回指标。

这个项目里我主要做了 Python 后端、RAG 检索、模型 API 封装、LangGraph 工作流编排，以及问答助手的反馈闭环和执行链路工程化。对我来说，这个项目最有价值的地方不只是“接了大模型”，而是把 `RAG + 问答 + 脚本生成 + 可选执行` 做成了一个可落地的内部提效工具原型。

### 14.2 90 秒版本

我这个项目本质上是一个垂直领域的 RAG 应用，知识源是 TED 用户文档、API 文档和参考设计。因为这类文档是 HTML 站点，不是干净文本，所以我在数据层先做了正文抽取、噪声过滤、标题感知切块和表格保留，再做 embedding 和索引持久化。

检索层我没有只用向量，而是做了混合检索。先做 embedding 召回，再做 BM25-like 的 lexical 补充召回，最后用 rerank 做精排。这样既能处理自然语言语义，也能更稳地命中 `Measure.ac`、`test_pvt` 这种 API 名和强术语。对于问答助手，我还做了一个拒答门控，比如 evidence overlap 太低或者 top1 分数不够时直接返回“未检索到相关内容”，这样可以降低幻觉风险。

在能力层面，项目有两个接口非常核心。一个是 `/v1/rag/ask`，也就是 TED 文档问答助手；另一个是 `/v1/tasks/run`，它会按 `prepare -> retrieve -> codegen -> preflight -> execute` 这条工作流运行。现在真实执行模板已经打通了带宽测试场景，系统会根据证据生成脚本，调用 `TED_BANDWIDTH_CMD` 执行，并校验 `result.json` 里的 `bandwidth_hz`。

所以如果从岗位匹配度来看，这个项目比较完整地覆盖了 Python、FastAPI、RAG、模型 API 调用、智能助手和工程化落地这些能力。

### 14.3 如果面试官只给你 30 秒

这是一个把 TED 文档做成 RAG 知识库的 AI 应用项目。我做了两个核心能力：一个是 `TED文档问答助手`，一个是基于文档证据自动生成测试脚本并可选执行的任务链路。重点不只是接入大模型，而是把文档解析、混合检索、拒答、反馈闭环和执行结果校验做成了一个可用的工程原型。

---

## 15. 高频追问 10 题，口语化回答版

下面这部分适合你在面试前重点看一遍，基本都是高频追问。

### 15.1 你在这个项目里最核心的贡献是什么？

我最核心的贡献主要有三块。第一块是 RAG 这条链路，包括 TED 文档解析、切块、向量化、混合检索和 rerank。第二块是 `TED文档问答助手`，我做了拒答门控、运行时缓存、证据展示和反馈落库。第三块是任务执行链路，也就是用户输入自然语言需求后，系统自动检索、生成脚本、做 preflight 检查并可选执行。

### 15.2 这个项目最难的点是什么？

我觉得最难的点是把垂直文档场景下的 RAG 做稳。因为 TED 文档不是纯文本，而是 HTML 站点，里面有导航噪声、参数表、代码块、API 说明和参考设计。如果文档解析和切块做不好，后面的 embedding、rerank 和生成都会受影响。所以我在 HTML 正文抽取、标题层级分段和表格保留上花了比较多精力。

### 15.3 为什么要做混合检索，而不是只做向量检索？

因为这个场景里有很多强标识符，比如 `Measure.ac`、`test_pvt`、`layout_mos`，这类术语用 BM25 或关键词匹配往往比纯向量更稳定。如果只靠向量，有时能理解语义，但不一定能精准命中 API 名。所以我采用的是“向量负责语义、BM25 负责术语、rerank 负责精排”的混合方案。

### 15.4 你怎么降低大模型幻觉？

我不是只靠 prompt 去压幻觉，而是做了多层控制。先通过混合检索和 rerank 提高证据质量；然后在问答前做一次门控，如果检索 overlap 太低就直接拒答；再在 prompt 里明确要求只能基于 evidence 回答；最后把 evidence 展示给用户，并记录“有用/无用”反馈，方便继续做 bad case 优化。

### 15.5 为什么问答助手要做拒答？

因为企业内部知识问答和聊天机器人不一样，错答的成本更高。像 TED 这种工具文档，如果系统把 API、参数或测试步骤答错，会直接影响工程师后续工作。所以我宁可让系统在低置信度下返回“未检索到相关内容”，也不想让它强行给一个看起来像对的答案。

### 15.6 你为什么没有直接用向量数据库？

这是一个 Phase-1 原型，知识源是本地 TED 文档，规模可控，所以我先用了 JSON 形式的本地持久化索引。这样做的好处是轻量、好调试、对内网部署也友好。后续如果语料规模和并发上来，再切到 FAISS 或 Milvus 会更合适。

### 15.7 LangGraph 在这个项目里起什么作用？

我不是把 LangGraph 当“炫技工具”，而是把它当状态机式工作流框架来用。当前任务链路比较固定，就是 `prepare -> retrieve -> codegen -> preflight -> execute`。用图来表达节点和状态，比把所有逻辑堆在一个函数里更清晰，也方便后续扩展人工审核、重试或者新工具节点。

### 15.8 这个项目里你怎么体现工程化能力？

我觉得主要体现在这几个点：有配置中心，有健康检查，有索引持久化，有 embedding/rerank fallback，有运行时缓存和 warmup，有 QA 的 timing log，有用户反馈落库，还有 smoke check 和 retrieval regression。也就是说，我不是只做了一个能跑的 demo，而是尽量把它做成一个可持续迭代的系统。

### 15.9 如果让你继续优化，你先做什么？

我会优先做四件事。第一，把向量索引升级成更成熟的向量存储。第二，把真实执行模板从带宽扩展到 SFDR、环路增益这些场景。第三，基于现有 `qa_logs` 和 bad case 持续优化 chunking、rerank 和拒答阈值。第四，补充更系统的离线评测和在线观测。

### 15.10 这个项目和恒生岗位最匹配的地方是什么？

我觉得最匹配的是，这个项目不是停留在“调用一下 LLM API”，而是完整覆盖了 Python 后端、FastAPI、RAG、智能助手、模型接口封装、数据处理和工程化落地。恒生这个岗位强调的是 AI 应用探索、提效工具落地和 RAG/智能助手经验，而我这个项目正好就是围绕这些点展开的。

---

## 16. 10 个更尖一点的追问，你可以这样接

### 16.1 如果面试官问：“你这个项目算 Agent 吗？”

你可以答：

严格来说，我更愿意把它叫“可控的 Agent Workflow”。因为它不是开放式自治系统，而是一个由 LangGraph 编排的状态机式流程，模型主要参与需求理解和代码生成，工具调用和执行边界都是我在工程上明确限定的。

### 16.2 如果面试官问：“为什么不用 function calling / tool calling？”

你可以答：

这个项目当前更适合固定流程，因为任务场景比较清晰，主要就是检索、生成和执行。相比完全开放的 tool calling，我更希望先把执行边界、结果契约和失败处理做稳。后续如果要扩展多工具决策，再引入 function calling 会更合适。

### 16.3 如果面试官问：“RAG 评测你怎么做？”

你可以答：

我分了三层。第一层是 smoke check，确认服务和索引基础可用；第二层是 retrieval regression，对运放带宽、GBW/PM、SFDR、loop gain、Measure.ac 这些核心 query 做检索回归；第三层是 QA 测试，会统计 `unsupported_claim_rate`、`abstain_precision` 和 `topic_drift_rate` 这类指标。

### 16.4 如果面试官问：“为什么要把表格转 markdown？”

你可以答：

因为 TED 文档里很多 API 说明是参数表，如果把表格打平，字段和默认值之间的关系就丢了。转成 markdown table 之后，检索和 evidence 展示都能更好地保留结构，对参数级问答更友好。

### 16.5 如果面试官问：“为什么 chunk 要带 Section 前缀？”

你可以答：

因为很多段落正文本身信息不全，要结合上层标题才知道它在讲什么。比如同样是“参数说明”，如果没有 `Section: tedcore::Generator > test_pvt` 这样的上下文，模型和检索器都很难判断这段到底属于哪个 API。

### 16.6 如果面试官问：“你怎么看待拒答和召回率的 trade-off？”

你可以答：

这是典型的精度和覆盖率 trade-off。我的选择是企业工具场景下优先保精度，所以对低置信度结果做拒答。因为对内部知识助手来说，答错的风险通常比少答更高。后续可以通过 bad case 优化召回和重排，在保证精度的前提下逐步提升覆盖率。

### 16.7 如果面试官问：“为什么执行器要校验 result.json，不直接看 stdout？”

你可以答：

stdout 更像日志，不适合作为强契约。`result.json` 是结构化结果，我可以明确校验 `status`、`metrics`、`bandwidth_hz`、`notes` 这些字段，保证后续链路消费时更稳定。这个设计本质上是把 LLM 生成脚本和执行器之间做了一个明确协议。

### 16.8 如果面试官问：“为什么只支持带宽的真实执行？”

你可以答：

因为我当前的目标是先把链路闭环验证跑通，而不是一次性把所有任务类型都接满。带宽测试是比较典型、可度量、容易定义结果契约的场景，所以我先选它做真实模板接入。这样既能证明执行链路可行，也方便后续按同样模式扩到 SFDR 和环路增益。

### 16.9 如果面试官问：“你怎么理解企业里的 AI 提效工具？”

你可以答：

我理解的企业 AI 提效工具，不是做一个看起来聪明的聊天框，而是把高频重复、依赖知识检索和流程执行的工作环节拆出来，做成一个可解释、可追溯、可反馈、可持续优化的系统。这个项目里，我就是围绕“查 TED 文档、生成测试脚本、执行测试”这个重复流程去做提效。

### 16.10 如果面试官问：“你最大的收获是什么？”

你可以答：

我最大的收获是，做 AI 应用最关键的不只是模型效果，而是系统设计。比如数据清洗怎么做、检索怎么设计、什么时候该拒答、失败怎么降级、结果怎么验证、反馈怎么沉淀。这些工程问题如果没处理好，大模型能力很难真正落地。

---

## 17. 最后给你的使用建议

面试前建议你按这个顺序准备：

1. 先背 `14.1 60 秒版本`
2. 再熟悉 `4. RAG 部分` 和 `5. TED 文档问答助手怎么讲`
3. 然后重点看 `9. 大模型面试八股文`
4. 最后把 `15` 和 `16` 里的高频追问过一遍

如果时间不够，最少要保证你能流畅说出这四句话：

1. 这是一个面向 TED 文档的垂直领域 RAG 项目，不是通用聊天机器人。
2. 我做了混合检索：embedding 召回、BM25 补充和 rerank 精排。
3. 我做了 TED 文档问答助手，并通过拒答、证据展示和反馈落库控制幻觉。
4. 我还把“检索 -> 生成脚本 -> preflight -> 执行 -> 返回指标”的链路打通了，目前真实执行已支持带宽测试。
