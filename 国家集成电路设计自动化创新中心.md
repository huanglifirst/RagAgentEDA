# 国家集成电路设计自动化创新中心项目介绍

## 一、项目背景

我在国家集成电路设计自动化创新中心参与的是 Agent EDA 自动化代码生成与执行系统中的 iAnalog 项目，岗位是 Agent 实习生。

公司原有系统已经具备一定的自动化能力：用户输入电路相关需求后，系统可以生成测试文件，执行测试流程，并返回对应的测试指标。但是原有知识库主要建立在精选测试代码之上，覆盖范围和可解释性相对有限。

团队希望验证另一种技术路径：直接以 TED 技术文档作为知识源，让 Agent 在问答、测试辅助和代码生成前，能够从正式技术文档中检索到可靠依据。这样可以提升系统对长技术文档场景的适配能力，减少模型幻觉，并增强下游代码生成的稳定性。

我的主要工作集中在 RAG 检索模块，包括多格式技术文档解析、知识库构建、检索链路设计、拒答门控和 QA 回归评测机制。

## 二、项目目标

这个项目的核心目标是打通从技术文档到 Agent 问答和代码生成支持的完整链路。

具体来说，需要解决以下问题：

1. 将 HTML、Markdown 等技术文档解析成结构清晰、可检索的知识片段。
2. 构建适合 EDA 技术文档的知识库索引，支持函数名、参数名、配置项等精确检索。
3. 结合向量召回、BM25 召回和 rerank 精排，提高检索相关性。
4. 在证据不足时让系统拒答，避免模型编造答案。
5. 通过 QA 回归评测持续优化检索链路，提升问答和代码生成稳定性。

## 三、整体技术链路

整个 RAG 链路可以概括为：

```text
TED 技术文档
  -> HTML / Markdown 解析
  -> 正文抽取与噪声过滤
  -> 标题、段落、代码块结构识别
  -> 文档切分与滑窗补偿
  -> 知识片段元数据构建
  -> 向量索引 + BM25 索引
  -> query 检索
  -> 多路召回结果融合
  -> rerank 精排
  -> 拒答门控
  -> 大模型生成答案或辅助代码
  -> QA 回归评测
```

这条链路的重点不是简单把文档丢给向量数据库，而是围绕 EDA 技术文档的特点，对解析、切分、检索、排序和质量控制做针对性优化。

## 四、文档解析与知识库构建

### 1. 多格式文档解析

项目中的技术资料主要包括 HTML 和 Markdown 文档。这些文档通常包含标题、段落、表格、代码块、API 参数说明和示例代码。

我负责搭建文档解析流程，将原始文档转换为统一的中间结构。解析时不仅提取正文内容，还会保留文档层级信息，例如一级标题、二级标题、接口名称、参数说明和代码示例。

例如一个接口文档可能包含：

```text
run_simulation
功能：启动仿真任务
参数：
- netlist_path：网表文件路径
- corner：工艺角配置
- timeout：最大等待时间
示例：
run_simulation(netlist_path="demo.sp", corner="tt", timeout=300)
```

如果只抽取纯文本，会丢失标题和参数之间的结构关系。因此解析阶段需要尽量保留文档结构，方便后续切分和检索。

### 2. 正文抽取与噪声过滤

技术文档中会有一些与问答无关的内容，例如导航栏、目录、页脚、版权信息、重复链接等。这些内容如果进入知识库，会干扰检索结果。

因此解析后需要做噪声过滤，去掉无意义内容，保留真正有价值的正文、接口说明、参数解释和示例代码。

### 3. 标题和段落边界优先切分

技术文档不能简单按固定长度切分。因为 EDA 文档中很多知识点依赖上下文，比如一个 API 的功能、参数、返回值和示例代码应该尽量保留在同一个知识片段中。

所以我的切分策略是优先按照标题、段落、列表和代码块边界切分，而不是机械地按字符数截断。

例如：

```text
标题：run_simulation 接口
段落：功能说明
列表：参数说明
代码块：调用示例
```

这些内容会尽量作为一个完整 chunk 保留。只有当内容超过长度限制时，才进一步切分。

### 4. 滑窗补偿

当文档内容过长必须拆分时，相邻 chunk 之间会保留一定 overlap，也就是滑窗补偿。

这样做是为了避免关键信息刚好被切在边界两侧。例如一个 chunk 里有接口名称，另一个 chunk 里有参数说明，如果没有 overlap，检索时可能只召回其中一部分，导致证据不完整。

通过滑窗补偿，可以提高长技术文档场景下的检索连续性。

## 五、检索链路设计

项目中采用的是混合检索思路，也就是向量检索、BM25 检索和 rerank 精排结合。

```text
用户 query
  -> query 预处理
  -> 向量检索召回
  -> BM25 关键词召回
  -> 候选结果合并
  -> rerank 精排
  -> 返回高质量证据片段
```

### 1. 向量检索的作用

向量检索主要解决语义相似问题。

例如用户问：

```text
如何限制仿真任务的最长运行时间？
```

文档中可能写的是：

```text
timeout 参数用于设置仿真任务的最大等待时间。
```

这两个表达字面不完全一样，但语义接近。向量检索可以把这类相关片段召回。

### 2. BM25 的作用

BM25 是关键词检索算法，主要看用户 query 中的词和文档 chunk 中的词是否字面匹配。

在 EDA 场景中，很多关键信息是函数名、参数名、配置项和测试命令，例如：

```text
run_simulation
netlist_path
timeout
corner
testbench
```

这些词往往需要精确命中。如果只用向量检索，模型可能把语义相似但 API 不同的内容召回。例如用户问 `run_simulation`，向量检索可能召回 `run_analysis`，因为它们都和仿真流程有关。

BM25 的优势是只要 query 中出现了 `run_simulation`、`timeout` 这类关键词，它就会优先召回包含这些字面词的文档片段。

需要注意的是，BM25 本身并不会理解“这是函数名”或“这是参数名”。它依赖 query 预处理和分词结果。因此在项目中，需要对技术 token 做保留和规范化，例如保留 snake_case、camelCase、下划线、API 名、参数名等。

一个典型例子：

```text
用户问题：run_simulation 的 timeout 参数默认值是多少？
```

query 预处理后可以得到：

```text
run_simulation
timeout
默认值
```

BM25 会用这些关键词去匹配知识库 chunk。如果某个 chunk 同时包含 `run_simulation`、`timeout` 和 `默认值`，它的得分就会更高。

### 3. rerank 精排

BM25 和向量检索负责从知识库中召回一批候选片段，但初始召回结果不一定都能真正回答问题。

rerank 的作用是对候选片段重新排序，判断每个片段和用户问题的相关程度。

例如用户问：

```text
run_simulation 的 timeout 参数默认值是多少？
```

初召回可能拿到很多包含 `run_simulation` 的片段：

```text
片段 A：run_simulation 的功能说明
片段 B：run_simulation 的参数列表
片段 C：run_simulation 的 timeout 默认值说明
片段 D：run_simulation 的返回值说明
```

rerank 会进一步判断哪个片段最能回答“timeout 默认值是多少”，并把片段 C 排到更靠前的位置。

## 六、拒答门控机制

RAG 系统中一个很重要的问题是：检索不到可靠证据时，模型可能会根据自身知识或语言模式编造答案。

为了降低幻觉，我在链路中加入了拒答门控。它的核心思想是：只有当检索结果满足一定证据条件时，才允许模型生成确定性回答；否则应该拒答、提示资料不足，或者建议用户补充文档。

### 1. 拒答门控判断依据

拒答门控可以结合以下信号：

1. top1 或 top-k 检索结果的 rerank 分数是否达到阈值。
2. 用户问题中的关键实体是否被检索结果覆盖。
3. 检索片段是否包含回答问题所需的核心信息。
4. 多个证据片段之间是否存在一致性。
5. 问题是否超出当前知识库范围。

例如用户问：

```text
run_simulation 的 timeout 默认值是多少？
```

如果检索结果只显示：

```text
timeout 参数用于控制仿真任务的超时时间。
```

但没有任何片段说明默认值，那么系统不能直接生成“默认值是 300 秒”。正确做法是提示：

```text
当前文档中只检索到 timeout 参数用途说明，未找到默认值定义，无法确认其默认值。
```

### 2. 拒答门控的价值

拒答门控可以让系统在证据不足时保持保守，避免生成没有依据的答案。对于 EDA 自动化代码生成场景，这一点尤其重要，因为错误的参数、函数名或默认配置可能会直接导致测试脚本执行失败。

## 七、QA 回归评测机制

QA 回归评测机制可以理解为给 RAG 系统准备一套固定问答测试集。每次调整文档切分策略、BM25 权重、向量召回参数、rerank 阈值或拒答门控后，都用这套测试集重新评估，避免优化某些 case 的同时破坏整体效果。

### 1. 测试样例设计

测试集通常包括两类问题。

第一类是应该正常回答的问题，例如：

```json
{
  "question": "run_simulation 的 timeout 参数有什么作用？",
  "gold_answer": "timeout 用于设置仿真任务的最大等待时间。",
  "gold_evidence": "timeout 参数用于设置仿真任务的最大等待时间。",
  "should_refuse": false
}
```

第二类是应该拒答的问题，例如：

```json
{
  "question": "run_simulation 是否支持 auto_magic_mode 参数？",
  "gold_answer": "文档中未找到 auto_magic_mode 参数说明，无法确认支持。",
  "gold_evidence": null,
  "should_refuse": true
}
```

### 2. 评测指标

常用评测指标包括：

1. Recall@K：正确证据是否出现在前 K 个检索结果中。
2. MRR：正确证据的排名是否足够靠前。
3. Answer Faithfulness：生成答案是否忠实于检索证据。
4. Refusal Accuracy：该拒答时是否拒答。
5. 误拒率：本来可以回答的问题是否被错误拒答。
6. 漏拒率：本来应该拒答的问题是否被错误回答。

通过这些指标，可以持续判断 RAG 链路是否真的变好，而不是只凭个别测试样例主观判断。

## 八、前端问答助手

除了 RAG 检索链路，我还配合搭建了一个面向测试人员的前端问答助手。

这个助手的目标不是替代测试人员，而是作为辅助型工具，帮助用户快速查询 TED 技术文档中的知识，例如接口用法、参数含义、测试配置说明和常见流程问题。

前端问答助手主要包括：

1. 用户问题输入。
2. RAG 检索结果展示。
3. 模型生成答案展示。
4. 文档证据来源展示。
5. 拒答或证据不足提示。

通过这个问答助手，测试人员可以更快定位文档知识，减少手动查阅长技术文档的时间。

## 九、项目难点

### 1. 技术文档结构复杂

HTML 和 Markdown 文档中既有正文，也有表格、列表、代码块和导航内容。如果解析不准确，会影响后续切分和检索。

### 2. 长文档切分容易破坏上下文

如果 chunk 太小，API 名、参数说明和示例代码可能被切开；如果 chunk 太大，检索精度会下降。因此需要结合标题、段落边界和滑窗补偿进行平衡。

### 3. EDA 术语需要精确匹配

函数名、参数名和配置项不能只依赖语义相似。BM25 在这里起到了补充作用，保证关键技术 token 能被准确召回。

### 4. 需要控制模型幻觉

技术文档问答不能在证据不足时强行回答，因此需要拒答门控和证据约束。

### 5. 检索优化需要可量化评估

RAG 参数调整很多，如果没有 QA 回归评测机制，很难判断改动是否真正提升整体效果。

## 十、项目成果

通过该项目，我完成了从 TED 技术文档语料到问答支持链路的打通，构建了 10k+ 知识片段索引，并结合向量召回、BM25 召回和 rerank 精排提升检索相关性。

同时，通过拒答门控和 QA 回归评测机制，系统在面对证据不足或知识库范围外问题时能够更稳健地处理，减少无依据回答。

整体来看，该项目为 Agent EDA 自动化代码生成与执行系统补充了一条基于正式技术文档的知识增强路径，也为后续测试辅助和代码生成场景提供了更可靠的知识基础。

## 十一、面试表达版本

可以用下面这段话进行项目介绍：

> 我在国家集成电路设计自动化创新中心参与的是 iAnalog Agent EDA 项目，主要负责自动化代码生成与执行系统中的 RAG 检索模块。原有系统已经可以根据电路生成测试文件、执行测试并返回测试指标，但知识库主要来自精选测试代码。团队希望验证另一条路径，也就是直接以 TED 技术文档作为知识源，为技术问答和代码生成提供更可靠、可追溯的依据。
>
> 我的工作包括 HTML、Markdown 技术文档解析，正文抽取，噪声过滤，标题和段落边界优先切分，滑窗补偿，以及知识片段索引构建。检索侧采用向量召回和 BM25 召回结合的方式，向量检索解决语义相似问题，BM25 解决函数名、参数名、配置项等精确匹配问题，然后通过 rerank 对候选片段进行精排。
>
> 为了降低模型幻觉，我还加入了拒答门控和 QA 回归评测机制。拒答门控会根据检索分数、关键实体覆盖和证据完整性判断是否允许生成答案；如果证据不足，系统会提示文档中未找到可靠依据。QA 回归评测则是建立一批固定问答样例，每次调整切分、检索或 rerank 参数后重新评估 Recall@K、MRR 和拒答准确率。最终这个项目打通了从 TED 技术文档到问答助手和代码生成支持的完整链路，提升了长技术文档场景下的检索相关性和下游生成稳定性。
